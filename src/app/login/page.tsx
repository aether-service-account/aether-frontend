import {Login} from "@/components/organisms/login";

export default function LoginPage() {
    return <main className="container flex min-h-screen flex-col items-center justify-between p-24">
        <Login/>
    </main>
}